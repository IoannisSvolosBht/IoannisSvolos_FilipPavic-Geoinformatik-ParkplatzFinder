<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.plcoding.mapscomposeguide">

    <application
        android:name=".ParkingSpotApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapsComposeGuide">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MapsComposeGuide">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        //Google Maps API Key erstellen und hier einfügen
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyCSjCtza4lXWkQezDQZadCR4H0TiF3QuUc"/>
    </application>

</manifest>